{% extends 'admin/base.html.twig' %}

{% block title %}Modifier l'Étage{% endblock %}

{% block body %}
    <h1>Modifier l'Étage</h1>

    {{ include('admin/etage/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('admin_etage_index') }}" class="btn btn-secondary">Retour à la liste</a>
        {{ include('admin/etage/_delete_form.html.twig') }}
    </div>
{% endblock %}
