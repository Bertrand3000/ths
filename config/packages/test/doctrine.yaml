doctrine:
    dbal:
        driver: 'pdo_sqlite'
        charset: utf8mb4
        # Temporary file database for tests (instead of memory for schema persistence)
        url: 'sqlite:///%kernel.project_dir%/var/test.db'
